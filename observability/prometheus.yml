global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  # Self-monitoring
  # Not very useful when running development, so its disabled
  # - job_name: 'prometheus'
  #   static_configs:
  #     - targets: ['localhost:9090']

  - job_name: 'storage_provider'
    static_configs:
      - targets: ['host.docker.internal:8001']
    metrics_path: /metrics
